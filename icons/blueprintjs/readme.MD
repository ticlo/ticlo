## Blueprint JS SVGs

These raw 16px SVGs are vendored so our Sass build can inline the `svg-icon("16px/..." )` assets that Blueprint references. The icons are pulled straight from the Blueprint repo until Palantir publishes them via npm.
